{
  "name": "discordword",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "deploy-commands": "bun run ./src/discord/deploy-commands.ts",
    "init-prisma": "bunx prisma migrate dev --name init && bunx prisma generate"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "5.3.1",
    "discord.js": "^14.13.0",
    "node-cron": "^3.0.2",
    "prisma": "^5.3.1"
  }
}